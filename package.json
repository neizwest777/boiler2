{
  "name": "BoileriABI.ee",
  "description": "BoileriABI.ee – Torumees Tallinnas & Harjumaa piirkonnas: boilerite paigaldus, remont ja hooldus.",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run appinfo && cross-env PORT=6006 NODE_OPTIONS='--no-warnings' next dev",
    "build": "npm run appinfo && next build && cross-env NODE_OPTIONS='--experimental-json-modules' node ./scripts/postbuild.mjs",
    "serve": "next start",
    "analyze": "cross-env ANALYZE=true next build",
    "lint": "next lint --fix --dir pages --dir app --dir components --dir lib --dir layouts --dir scripts",
    "pretty": "prettier --write .",
    "appinfo": "node ./scripts/generateAppInfo.mjs"
  },
  "dependencies": {
    // ... существующие зависимости без изменений
  },
  "devDependencies": {
    // ... существующие devDependencies без изменений
  },
  "overrides": {
    "react": "^19.1.1",
    "next": "^15.5.3"
  },
  "resolutions": {
    "react": "^19.1.1",
    "next": "^15.5.3"
  },
  "lint-staged": {
    "*.+(js|jsx|ts|tsx)": [
      "eslint --fix"
    ],
    "*.+(js|jsx|ts|tsx|json|css|md|mdx)": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "browserslist": {
    "production": [
      "last 2 chrome versions",
      "last 2 firefox versions",
      "last 2 safari versions", 
      "last 2 edge versions"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
